# 版本日志

## 1.3 Beta · 2026-08-20

本版本主要完善 macOS 原生界面、技能创建流程和技能生命周期管理。它仍是核心功能原型，不代表 OCR、文件操作等底层工具已经全部可用。

### UI 与交互

- 设置窗口改为左侧分类、右侧详情的 macOS 原生布局，使用全尺寸透明标题栏与 Liquid Glass 材质。
- 设置页加入前进/后退历史导航；标题分隔线只在内容发生滚动后显示，不可滚动页面保持隐藏。
- 技能管理采用列表与二级编辑页，避免多列布局把窗口过度撑宽。
- 调整技能编辑标题、按钮点击范围、禁用状态灰度和整体间距。
- 主面板预测项显示真实调用格式，并保持 Spotlight 风格的内联补全、Tab 切换和回车执行。
- AI 回答取消独立深色结果卡片，统一融入主内容区域。

### 技能创建与参数

- 引导模式改为四步流程：名称与关键词、输入参数、处理过程、输出形式。
- 自由模式增加独立的注册关键词和参数定义区域。
- 支持文本、文件、图片、文件夹、数字和布尔值参数，并区分必填与可选。
- 快捷面板可以从命令文本、Finder 拖拽、剪贴板和原生文件选择器收集运行时参数。
- 收集文件期间临时固定面板，完成或取消后恢复失去焦点自动隐藏。

### 技能管理与分享

- 支持搜索、编辑、启用或停用、删除已保存技能。
- 可编辑名称、别名、说明、执行模式、步骤、参数和云端模型提示词。
- 支持导出带格式标识和 Schema 版本的 `.laskill` 技能包；不包含 API Key、生成历史或运行时文件。
- 保存和删除失败时恢复内存状态，避免界面与磁盘数据不一致。

### 模型输出与兼容性

- 主面板问答、技能生成过程、技能解释和历史记录统一使用安全 Markdown 渲染。
- 代码和未解析 JSON 使用独立代码视图，支持选择、复制和横向滚动。
- 工作流参数升级为递归 JSON 类型，兼容字符串、数字、布尔值、数组、对象和 `null`。
- 加强技能生成提示词约束，避免把 `modelTask` 声明错误地嵌入工作流参数。
- JSON 解码失败时显示具体字段路径和类型原因，便于定位模型格式问题。

### 当前限制

- 文件和图片参数目前只完成接收、类型校验与运行上下文绑定。
- OCR、文件读取与整理、剪贴板工具、系统诊断、Spotlight 动态技能、本地模型尚未接入。
- `.laskill` 当前支持导出，导入、签名、版本更新和社区分发仍待开发。
- GLM 和 OpenAI 仍属于实验性接口；当前主要使用 DeepSeek 开发和验证。

## 1.2 Beta · 2026-08-20

- 加入透明的流式技能生成过程、耗时与 Token 统计、终止按钮和本地生成历史。
- 创建新技能时隔离旧草稿状态，补充应用内 Console 和详细错误日志。
- 初步区分本地执行技能与云端 AI 技能。

## 1.0 Beta · 2026-08-20

- 接入 DeepSeek 技能生成原型。
- 支持生成、预览并保存技能定义。

## Demo

- 完成菜单栏入口、快捷面板和设置窗口的首版 UI 原型。
