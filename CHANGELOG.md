# 版本日志

## 1.7 Beta · 2026-08-21

### 命令与参数

- 主面板建立统一命令语法：`唯一索引 参数1 参数2 … 条件`，不再把技能名称或搜索别名误当作可执行命令。
- `Tab` 与方向键只切换预测候选，空格确认候选后自动补齐唯一索引，并在输入框光标后用灰字提示下一个参数；参数完整时回车直接执行。
- 参数解析支持引号、转义、文本、段落文字、数字和文件类参数；数字会在执行前校验，文件仍可拖拽、粘贴或使用原生文件选择器传入。
- 新增首个运行条件 `-clear`：条件只控制本次调用，不会传入工作流或模型；任务完成、失败或取消都不会写入调用历史。

### 权限、安全与本地数据

- “隐私与权限”改为真实权限中心：显示辅助功能与屏幕录制的当前系统状态，可直接请求授权或进入对应系统设置，并在应用重新激活时刷新。
- 文件访问改为用户逐个选择目录并保存安全书签；已授权位置可查看、移除，并已接入本地文件搜索范围。
- API Key 默认继续保存到 macOS 钥匙串，并使用“设备解锁时可访问”级别；用户也可确认后迁移到 Application Support 中权限为 0600 的可查看 JSON 文件。
- 本地密钥模式在 AI 服务页提供显示/隐藏按钮；切换回钥匙串会先写入钥匙串，成功后删除本地明文文件。

### 设置与界面

- 统一设置窗口所有页面的标题栏、Form、List、空状态和底部操作栏背景；滚动容器不再各自绘制不同底色，浅色与深色模式下都使用同一动态系统语义色。
- 技能搜索框移入技能管理标题栏右侧，缩短为紧凑尺寸并使用交互式 Liquid Glass 材质，减少列表上方的无效占高。
- 历史记录与技能管理的标题栏统一为 58pt，导航按钮和搜索框均为 36pt 并垂直居中；搜索占位符直接显示当前记录或技能总数。
- 历史和技能列表改为苹果设置式整体圆角分组，使用行间分隔线与右侧箭头；技能列表移除末尾状态圆点。
- “创建技能”“全部清空”和“多选”改为右下角 Liquid Glass 悬浮操作；两个页面均支持多选、批量删除与批量导出。
- 修复快捷面板重复唤起时搜索框偶尔没有输入光标的问题；面板每次成为 key window 后都会重置并重新建立 SwiftUI/AppKit 焦点。
- Dock 图标收到重新打开事件时会直接唤起主助手面板；“登录时自动启动”改为使用 `SMAppService.mainApp` 真实注册，并显示系统批准状态和错误信息。
- 新增按参数类型自动切换输入法：技能关键词阶段使用英文输入源，确认技能后遇到文本或段落参数会恢复唤起前的第三方输入法；通用设置可关闭，面板结束时始终恢复原输入法。
- 设置窗口不再允许从侧栏内容区拖动，仅保留顶部空白区域用于移动窗口，避免浏览和点击设置时误拖窗口。
- 技能编辑二级页统一标题、表单、编辑器与底部操作栏的窗口底色；多行文本编辑器改为透明承接分组圆角容器，不再出现内外两种背景色。
- 设置侧栏改用应用主题色绘制选中状态，新增“跟随系统”主题色，可读取 macOS 当前强调色。
- 设置侧栏未选中项目的图标使用当前主题色；选中后图标与文字统一为白色，保证主题色背景上的对比度。
- 修复透明设置窗口左右分栏之间的 1px 穿透缝隙；分隔槽增加不透明窗口底色承托，同时保留细微系统分隔线。
- 快捷面板会记住用户最后一次拖动的位置；再次唤起或重新启动应用后从该位置显示，显示器布局变化时会自动校正到可见区域。
- 主面板底部在“创建技能”旁新增“设置”入口，可直接打开完整设置窗口。
- 普通 AI 问答和技能内容生成不再传入应用侧输出 Token 上限，由所选模型按自身能力完成回答；连接测试与结构化技能草稿仍保留任务所需的小范围限制。
- 生成期间搜索框提供终止按钮，Esc 会优先取消当前 API 或工作流任务而不关闭面板；完成后按钮恢复为更醒目的清空叉号。
- 搜索框移除模型服务商绿色状态胶囊；技能预测新增有序缩写匹配，例如输入 `ct` 可以找到索引为 `cliptrans` 的技能，同时保持精确和前缀结果优先。
- 移除内置 `move` 技能和 `file.move` 底层工具；升级时会清理旧版本保存的对应内置技能。
- 新增 `new 名称.后缀` 本地技能与 `file.createEmpty` 工具，默认在 Downloads 创建空文件；结果区显示文件卡片，可拖到 Finder、双击打开或移到废纸篓。
- 回答区不再重复显示顶部输入框中已经存在的命令；“复制结果”和“新问题”移到回答标题栏右侧，取代重复的执行来源胶囊，并删除底部操作行。
- `new` 的结果改为居中的近方形文件卡片，打开与红色删除操作放在卡片下方；删除不再二次确认，直接移到 macOS 废纸篓。
- `find` 搜索结果不再输出不可操作的 Markdown 路径，而是显示系统文件图标、名称和所在目录组成的交互列表；单击文件在 Finder 中定位、双击打开，文件夹则直接打开。

### 技能系统

- 技能编辑页新增实时“调用方式”、独立唯一索引和搜索别名字段；索引禁止空格、禁止以 `-` 开头，并在整个技能库中进行不区分大小写的唯一性校验。
- 参数名称与类型仍可编辑；参数改名时会同步迁移运行时模型提示词和工作流中的变量引用，保存后主面板预测及注册索引立即更新。
- 技能生成协议新增 `paragraph` 参数类型，并停止把唯一索引重复保存为搜索别名。

## 1.6 Beta · 2026-08-21

- 主面板运行状态改为读取真实工具链：Apple Vision OCR、本地文件、剪贴板、系统工具和云端模型分别显示对应动作，不再只要配置了 API 就统一写成 DeepSeek。
- 新增混合进度显示：Vision OCR 使用系统提供的真实识别进度，多步骤工作流显示整体完成百分比；无法确定总量的模型请求使用不定进度，避免伪造百分比。
- 搜索框服务标签和结果来源按实际执行范围显示“本地”、模型服务商或“模型 + 本地”，本地 OCR 不再错误标记为 DeepSeek。
- 主面板长回答的滚动区域增加动态边缘渐隐：滚动后顶部柔和淡出，尚有后续内容时底部淡出，到达顶部或底部会自动取消对应遮罩。
- 修复从浅色切回“跟随系统”后窗口仍混用浅色外观的问题；外观模式改由 AppKit 在应用级统一管理，系统模式会完整移除强制覆盖。
- 设置侧栏在浅色与深色模式下统一使用系统薄材质和 Liquid Glass 效果，设置窗口启用透明合成，右侧内容区保持系统窗口底色。
- 主面板按下清空按钮或手动删除全部输入后，会同步清除上一次回答、参数输入和执行状态，恢复到干净的推荐界面；正在运行的请求会被取消，已保存的历史记录不受影响。
- 悬浮窗移除搜索框外部的独立执行按钮，搜索/执行改为输入框内部的放大镜按钮，并在旁边提供清空按钮。
- 设置新增“历史记录”页面，调用输入、结果、状态、耗时和实际工具链保存在本机，支持查看详情、单条删除、搜索和全部清空。
- 删除独立“外观”分类，将外观和主面板设置迁入“通用”，新增紫、蓝、青、绿、橙、粉六种主题色。
- 新增剪贴板、选中文字、屏幕区域截图、Apple Vision OCR、文件读取/列表/搜索/重命名/移动/废纸篓和系统诊断工具。
- 文件修改、废纸篓和原位替换统一使用原生确认，文件操作限制在当前用户目录且不覆盖已有项目。
- 将 15 个默认能力保存为标准 Skill Definition，并在技能管理中标注“内置技能”。
- 内置技能与用户技能共用编辑、启停、导出和删除流程；删除后持久记录，不会在下次启动时强制恢复。
- 主面板不再展示另一套写死功能卡片，统一从技能库预测、补全和运行。
- 技能生成提示词更新为真实工具 ID 和参数结构，不再声明尚未实现的网络或通知工具。

## 1.5 Beta · 2026-08-21

- 快捷面板只允许从顶部留白区域拖动，正文和回答区域可以正常框选文字。
- 主面板改为单一输入线：`Tab` 或空格确认候选后锁定技能、补齐命令，并在同一输入框用灰字提示下一参数；下方只显示只读指引，参数齐全时首次回车直接执行。

## 1.4 Beta · 2026-08-21

- 建立最小 Tool Registry 与顺序 Workflow Engine。
- 实装 `clipboard.readText`、`model.generateText`、`clipboard.writeText`。
- 打通“读取剪贴板 → 云端翻译 → 显示并写回剪贴板”的真实工作流。
- 兼容没有 `workflow` 和 `modelTask` 的旧版 `clipboard + translate` 技能。
- 增加逐步耗时、错误传播和不记录正文的脱敏执行日志。
- 技能列表与编辑页纳入同一套前进/后退历史，返回列表后可以再次前进到刚才的技能。
- 普通模型任务明确要求直接输出 Markdown；模型误回纯 JSON 时自动转换为可读内容，明确要求 JSON 的技能不受影响。

## 1.3 Beta · 2026-08-20

本版本主要完善 macOS 原生界面、技能创建流程和技能生命周期管理。它仍是核心功能原型，不代表 OCR、文件操作等底层工具已经全部可用。

### UI 与交互

- 设置窗口改为左侧分类、右侧详情的 macOS 原生布局，使用全尺寸透明标题栏与 Liquid Glass 材质。
- 设置页加入前进/后退历史导航；标题分隔线只在内容发生滚动后显示，不可滚动页面保持隐藏。
- 技能管理采用列表与二级编辑页，避免多列布局把窗口过度撑宽。
- 调整技能编辑标题、按钮点击范围、禁用状态灰度和整体间距。
- 主面板预测项显示真实调用格式，并保持 Spotlight 风格的内联补全、Tab 切换和回车执行。
- AI 回答取消独立深色结果卡片，统一融入主内容区域。

### 技能创建与参数

- 引导模式改为四步流程：名称与关键词、输入参数、处理过程、输出形式。
- 自由模式增加独立的注册关键词和参数定义区域。
- 支持文本、文件、图片、文件夹、数字和布尔值参数，并区分必填与可选。
- 快捷面板可以从命令文本、Finder 拖拽、剪贴板和原生文件选择器收集运行时参数。
- 收集文件期间临时固定面板，完成或取消后恢复失去焦点自动隐藏。

### 技能管理与分享

- 支持搜索、编辑、启用或停用、删除已保存技能。
- 可编辑名称、别名、说明、执行模式、步骤、参数和云端模型提示词。
- 支持导出带格式标识和 Schema 版本的 `.laskill` 技能包；不包含 API Key、生成历史或运行时文件。
- 保存和删除失败时恢复内存状态，避免界面与磁盘数据不一致。

### 模型输出与兼容性

- 主面板问答、技能生成过程、技能解释和历史记录统一使用安全 Markdown 渲染。
- 代码和未解析 JSON 使用独立代码视图，支持选择、复制和横向滚动。
- 工作流参数升级为递归 JSON 类型，兼容字符串、数字、布尔值、数组、对象和 `null`。
- 加强技能生成提示词约束，避免把 `modelTask` 声明错误地嵌入工作流参数。
- JSON 解码失败时显示具体字段路径和类型原因，便于定位模型格式问题。

### 当前限制

- 文件和图片参数目前只完成接收、类型校验与运行上下文绑定。
- OCR、文件读取与整理、剪贴板工具、系统诊断、Spotlight 动态技能、本地模型尚未接入。
- `.laskill` 当前支持导出，导入、签名、版本更新和社区分发仍待开发。
- GLM 和 OpenAI 仍属于实验性接口；当前主要使用 DeepSeek 开发和验证。

## 1.2 Beta · 2026-08-20

- 加入透明的流式技能生成过程、耗时与 Token 统计、终止按钮和本地生成历史。
- 创建新技能时隔离旧草稿状态，补充应用内 Console 和详细错误日志。
- 初步区分本地执行技能与云端 AI 技能。

## 1.0 Beta · 2026-08-20

- 接入 DeepSeek 技能生成原型。
- 支持生成、预览并保存技能定义。

## Demo

- 完成菜单栏入口、快捷面板和设置窗口的首版 UI 原型。
