# 项目长期上下文与产品约束

> 最后更新：2026-08-21
> 工程位置：`/Users/wei/Projects/LocalAssistant`  
> 本文档用于保存已经讨论和确认的产品方向、技术决策、功能范围与注意事项。后续开发前应先阅读本文档，避免偏离目标。

## 1. 产品愿景

这是一个面向个人电脑的、本地优先的 macOS 智能工具。它不是传统聊天软件，也不应只是“另一个 AI Assistant”。

核心目标：

- 用户只需要表达意图，程序负责选择合适的工具并完成任务。
- 产品最重要的能力不是内置多少固定功能，而是让普通用户只用一句自然语言描述，就能创建、测试并保存属于自己的个人技能。
- 内置 OCR、翻译、文件、剪贴板和诊断等能力既是可直接使用的功能，也是用户技能可以安全组合的基础原语和学习示例，不是产品能力的最终边界。
- 能力边界由本地模型、系统 API 和已安装工具共同决定；增加模型或工具后，产品能力可以持续扩展。
- 把 Apple Silicon 平时闲置的本地计算能力转化为日常效率，但不能明显影响续航、内存和系统流畅度。
- 默认本地、私密、快速、安全、可预览、可撤销。
- 用户不应该为了一个几秒钟的小任务打开大型聊天应用。

暂定宣传方向：

```text
Ask anything. Make it happen.
Your intent, executed.
One command. Any task.
```

## 2. 产品名称

`LocalAssistant` 只是当前工程占位名称，不是最终品牌名。

已经讨论过的主要候选：

- `Praxis`：从意图到行动，目前综合推荐度最高。
- `Versa`：适应任何任务，消费级、容易理解。
- `Omnia`：一切、所有事物，最直接表达“什么都能做”。
- `Proteus`：能够随任务改变形态。
- `Forge`：把想法变成结果。

其他讨论过的名字包括：Vela、Summon、Lucent、Wisp、Vanta、Conduit、Vector、Nexus、Catalyst、Kinetic、Facet、Alloy、Axiom、Atlas、Sovereign、Daemon、Invoke、Impulse、Morph、Arsenal。

在正式改名之前必须进行：

- App Store 重名检查
- GitHub 项目重名检查
- 域名检查
- 商标与同类软件冲突检查

## 3. 已确认的产品形态与调用协议

### 3.1 Spotlight-first，同时保留轻量主界面

产品应优先集成到 macOS Spotlight：

```text
用户打开 Spotlight
→ 搜索某项动作
→ Spotlight 调用 App Intent
→ 简单任务直接返回结果
→ 复杂、耗时或修改性任务才弹出临时窗口
```

需要一个可以从菜单栏常驻图标和全局快捷键打开的主界面，但它不是传统仪表盘，也不是长期打开的聊天窗口。主界面应接近 Apple Spotlight：突出一个大的统一搜索/问答框，在下方只显示少量根据用户最近使用排序的推荐技能；提交请求后在同一面板显示执行过程和结果。

全局快捷键唤起的主面板必须是无标题栏、无红黄绿窗口按钮的临时浮层。面板失去键盘焦点时立即隐藏，`Esc` 也可关闭；再次使用快捷键时恢复显示并聚焦输入框。视觉结构可以借鉴 Raycast 的高密度单列结果体验，但不能照搬，应保留本项目自己的本地执行状态、技能推荐和回答结构。

快捷面板唤起时临时切换到已启用的英文输入源，避免输入 `ocr`、`find` 等命令时中文输入法先截获回车；面板隐藏或应用退出时恢复用户此前的输入源。输入内容变化时，应像 Spotlight 一样通过本地命令名、别名和描述实时预测工具，不调用大模型；最佳匹配排在首位，并在输入框内显示灰色补全后缀。回车执行当前候选，`Tab` 和上下方向键循环候选；未匹配内容仍可作为普通问题提交。

界面遵循 Apple 新一代 Liquid Glass 设计原则：玻璃主要用于搜索、按钮等交互控制层，正文和结果保持清晰的内容层；使用系统语义颜色、连续圆角和克制的强调色，不堆叠多层玻璃。macOS 26 及以上使用原生 Liquid Glass API，macOS 15–25 使用系统 Material 回退，保证部署兼容性。

准确的产品形态是：

- 存在一个标准 `.app` 作为 App Intents、权限、模型、设置和工具代码的载体。
- 平时不显示主窗口；用户从菜单栏、全局快捷键或系统入口调用时才创建或显示轻量主面板。
- Spotlight 是主要入口。
- 轻量主面板是完整自由输入、问答和任务结果入口，也是 Spotlight 失效时的可靠入口。
- 菜单栏图标暂时保留，用于设置、模型状态、任务状态和退出；未来允许用户关闭。
- 只有任务需要时才创建对应的临时界面。
- 主面板不能把所有功能平铺展示成卡片墙，但也不能空到只剩一个输入框；应通过最近使用、推荐技能、示例问题、加载状态和结果卡片让用户理解产品能力。

### 3.2 按任务显示临时窗口

未来不应继续扩展现在的“大型功能卡片主页”，而应改造成任务专用界面：

- OCR 结果窗口
- 翻译、总结、解释、改写结果窗口
- 文件操作预览与确认窗口
- 系统状态结果窗口
- 长任务进度窗口
- 模型下载窗口
- 权限引导窗口
- 通用自由指令窗口

简单动作尽量直接在 Spotlight、通知或剪贴板中返回结果。

### 3.3 首次启动与设置仍然需要 UI

首次启动可以显示轻量引导：

- 产品能做什么
- 如何从 Spotlight 运行动作
- 如何使用全局快捷键
- 模型下载情况
- 隐私和权限说明

设置页面至少需要：

- 模型管理
- 功耗模式
- 权限管理
- 隐私设置
- 索引目录
- 剪贴板保存策略
- 菜单栏图标开关
- 全局快捷键设置
- 操作历史与撤销

### 3.4 两条明确分离的执行路径

产品不是让所有输入都经过大模型。入口必须区分“用户已经明确指定工具”和“用户需要程序理解意图”两种情况。

#### 路径 A：直接命令，不调用通用语言模型

示例：

```text
la ocr
la translate
la summarize
la find 去年的租房合同
la clipboard 搜索刚才复制的地址
```

执行链：

```text
Spotlight / 快捷面板
→ 固定命令和别名解析
→ 统一 Tool Registry
→ Swift 工具函数
→ 直接结果或固定模板
```

这一路径必须优先追求确定性和速度。OCR 可以调用 Apple Vision 等机器学习能力，但不应因此加载通用语言模型。

#### 路径 B：`la ask` 自然语言理解

示例：

```text
la ask 电脑今天怎么这么卡
la ask 把这张截图里的英文提取出来并翻译
la ask 帮我找到昨天下载的合同并改成容易识别的名字
```

执行链：

```text
Spotlight / 快捷面板
→ 规则、关键词或 Embedding 召回 3～5 个候选工具
→ 小型专用模型选择工具并提取参数
→ JSON Schema 校验和安全策略
→ Swift 工具函数执行
→ 结构化结果
→ 固定模板或同一个小模型生成简明解释
```

第一版优先使用同一个模型完成“路由”和“结果解释”两次短推理，避免同时常驻两个模型。常见工具结果优先使用确定性模板，可以省掉第二次模型调用。

两条路径必须共享同一个面向用户任务的 Tool Registry 和同一套 Swift 工具实现，不能分别维护两套功能。内存、磁盘、进程、端口等底层读取能力属于内部 Probe Registry，只供诊断任务组合调用，不作为独立命令或模型可见工具暴露。

### 3.5 结果必须有证据边界

工具应输出结构化、可追溯的数据，模型只能根据真实字段解释结果。

例如，只有工具实际获得 Chrome 标签页数量时，产品才可以说“Chrome 开了太多标签页”。如果只知道 Chrome 相关进程共占用 8.4GB，只能说：

```text
Chrome 相关进程目前共占用约 8.4GB，可能来自网页、扩展或后台进程。
```

模型不得把推测包装成已经检测到的事实。

## 4. 核心功能范围

### 4.1 系统入口

- 全局快捷键唤起自由输入面板。
- 菜单栏图标。
- Spotlight App Intents。
- macOS Shortcuts 集成。
- Siri 集成可作为后续能力，不是第一阶段重点。

### 4.2 文字处理

- 获取用户选中的文字。
- 翻译。
- 总结。
- 解释。
- 改写。
- 调整语气和长度。
- 将结果复制或替换回原应用。
- 替换操作应尽量保留原应用自身的撤销能力。

选区获取策略：

1. 优先使用 macOS Accessibility API。
2. 无法读取时使用“保存剪贴板 → 模拟复制 → 读取 → 恢复原剪贴板”的回退方案。

### 4.3 本地文件

- 按文件名、类型、日期和元数据搜索。
- 按内容搜索文档。
- 语义搜索。
- 文件整理和分类。
- 批量重命名。
- 移动文件。
- 创建文件夹。
- 查找相似或重复文件。
- 文件问答和文件夹总结。
- 到期合同、发票、会议记录等内容提取。

修改文件时必须预览、确认并支持撤销。

### 4.4 剪贴板

- 剪贴板历史。
- 关键词搜索。
- 语义搜索。
- 收藏、固定和快速粘贴。
- 自动识别文本、链接、电话号码、地址等类型。
- OCR 图片剪贴板内容。

必须过滤或避免保存：

- 密码
- 验证码
- API Key
- 银行卡等敏感信息
- 密码管理器和隐私模式应用中的内容

### 4.5 截图、OCR 与图片文字

- 区域截图。
- 指定图片 OCR。
- 图片内文字复制。
- OCR 后翻译、总结、解释或保存。
- 截图按识别内容命名。

OCR 优先使用 Apple Vision/Core ML 等系统优化能力，不应为了 OCR 加载通用大语言模型。

### 4.6 系统状态与诊断

- 面向用户只提供“电脑为什么变卡”“网络为什么异常”“存储空间为什么不足”等问题导向的组合诊断。
- 不提供独立的内存、磁盘、CPU、进程和端口监视入口；这些信息在菜单栏监视器和系统工具中已经足够容易查看。
- 底层仍然实现内存压力、Swap、磁盘空间、CPU/GPU、进程、端口、网络、启动项等只读探针。
- 诊断任务根据问题组合调用多个探针，再把相关证据组织成结论和建议。
- 小模型只需要选择“性能诊断”等用户任务，不需要在大量低层系统函数中选择。

默认只诊断和解释。结束进程、修改启动项等操作必须确认。

### 4.7 自然语言工具路由

- 根据自然语言选择正确工具。
- 提取结构化参数。
- 判断任务是否需要模型。
- 支持多步骤任务，但第一阶段不追求复杂多智能体。

处理优先级：

```text
确定性规则
→ 系统 API / 普通搜索
→ 小型分类器
→ 轻量语言模型
→ 主语言模型
```

能够用规则或系统 API 完成的任务，不应唤醒生成模型。

### 4.8 自然语言创建个人技能（核心能力）

用户可以用一句话描述自己希望长期复用的能力，例如：

```text
把我选中的英文翻译成中文，提取三个重点，然后复制到剪贴板。

当下载目录出现新发票时，识别发票日期和公司名，先给我预览建议的新文件名。

每周五把本周保存的会议记录汇总成一个待办清单。
```

系统通过内置模型、用户选择的本地模型，或用户自己的 API，将描述转换为受约束的声明式 Skill Definition。创建过程必须支持追问缺失参数、生成可读执行计划、权限预览、样例测试和保存。保存后的技能立即进入自有快捷面板的搜索、别名和推荐系统，并在平台允许的范围内进入 Spotlight 索引。

模型只负责生成声明式计划，不能生成并直接执行任意 Shell、AppleScript 或未审查代码。技能必须只组合 Tool Registry 中已注册的安全动作、触发器、条件和输出；修改文件、发送消息或访问网络等行为继续遵守确认、权限、日志和撤销规则。

第一阶段只支持顺序执行和少量条件判断，不追求任意程序生成。内置功能要作为模板和示例，让用户理解“软件能替自己完成什么”，然后通过自然语言创建个人能力。

### 4.9 后续扩展功能

- 本地文档 RAG 和知识问答。
- 下载目录、桌面、截图目录的智能收件箱。
- 只提出整理建议，第一阶段不静默移动文件。
- 工作流录制、保存和重复执行。
- 定时工作流。
- 本地语音速记和语音转文字。
- 会议纪要、待办提取和录音关联。
- 隐私保护中心。
- 系统故障快速诊断。
- 主动智能：在用户明确授权后，通过事件和轻量探针发现异常、遗漏和待处理事项，再主动提醒用户。
- 系统异常通知，例如持续高负载、异常内存压力、磁盘空间快速下降或网络状态异常。
- 消息跟进提醒，例如支持的应用中出现用户尚未查看或尚未回复的重要消息，经过用户设定的时间后提醒。
- 用户可使用第三方大模型描述并生成自己的工作流或技能包，经过权限审查后导入本地运行。
- 主动智能和允许复杂第三方集成的技能包属于远期能力；受限的自然语言技能创建 MVP 应在核心工具和执行引擎稳定后尽早进入产品，不再视为纯远期附加功能。

## 5. Spotlight 集成设计

### 5.1 App Intents：执行动作

计划向 Spotlight 暴露：

- 打开应用/自由指令面板
- 询问产品
- OCR 识别图片
- 翻译文字
- 总结文字
- 解释文字
- 改写文字
- 搜索本地文件
- 搜索剪贴板
- 诊断电脑变慢或异常
- 整理指定目录

Spotlight 不能让第三方应用任意拦截全部原始查询，也不能保证像终端一样解析 `ocr xxx`。正确交互是：

```text
输入 OCR
→ Spotlight 找到“使用本应用识别图片文字”
→ 用户选择动作
→ Spotlight 继续收集图片等参数
→ 执行动作
```

动作名称、短语、别名和本地化应该覆盖 `OCR`、`文字识别`、`截图识别` 等常见表达。

因此，`la ocr` 和 `la ask ...` 是产品希望支持的统一命令语法，但必须接受以下平台现实：

- 在自有全局快捷键面板中，可以完全控制并稳定解析这一语法。
- 在 Spotlight 中，应主要通过 App Intents、动作别名和参数完成同等功能。
- 不能把“Spotlight 一定会把任意一行原始文本完整传给第三方应用”作为产品成立的前提。
- 如果系统匹配体验不够稳定，全局快捷键面板是精确命令入口，Spotlight 继续承担动作发现和快速启动。

### 5.2 Core Spotlight：搜索内容

未来可以选择性索引：

- OCR 历史
- 用户收藏的剪贴板项目
- 文件整理记录
- 保存的工作流
- 对话或任务标题
- 用户明确允许索引的文档

剪贴板和敏感数据不能默认全部进入 Spotlight 索引。必须提供开关和过滤。

### 5.3 第一项 Spotlight 动作

最初应先实现：

```text
打开应用
```

用于验证 App Intent 元数据、Spotlight 展示、菜单栏应用唤醒和系统兼容性。不要在功能尚未完成时注册大量不可用动作。

### 5.4 用户技能与 Spotlight 的平台边界

用户运行时创建的技能可以建模为 `UserSkillEntity`，遵循 `AppEntity`/`IndexedEntity` 并写入 Core Spotlight 索引。这样用户能够在系统 Spotlight 中搜索技能名称、别名和描述，点击后通过一个编译期存在的通用 `OpenUserSkillIntent` 或 `RunUserSkillIntent` 回到应用执行指定技能。Apple 官方支持在运行时索引 App Entity，并为实体提供打开入口：[Making app entities available in Spotlight](https://developer.apple.com/documentation/appintents/making-app-entities-available-in-spotlight)。

必须接受平台限制：App Intent 的类型和可执行代码由应用在编译期提供，不能让模型在运行时向系统注册任意新的原生 Intent 类型。Apple 的设计文档还明确说明 macOS 不支持 App Shortcuts，但 App Intents 动作仍可供 Mac 快捷指令使用：[App Shortcuts HIG](https://developer.apple.com/design/human-interface-guidelines/app-shortcuts)。

因此入口分工是：

```text
Option + Space 自有面板
→ 支持完整的动态技能搜索、参数补全、运行、编辑和结果展示

系统 Spotlight
→ 索引并发现用户技能实体
→ 点击后交给应用的通用执行入口
```

不能承诺每个用户技能都会变成一个独立的系统原生 App Intent，也不能让产品是否成立依赖 Spotlight 对动态动作的展示稳定性。

## 6. 技术路线

### 6.1 macOS 应用

- Swift
- SwiftUI
- 必要时使用 AppKit
- App Intents
- Core Spotlight
- Apple Vision
- SQLite
- 后续可能使用 XPC/后台服务隔离模型与工具执行

SwiftUI 主要负责：

- 设置
- 首次启动引导
- 临时结果与确认窗口

AppKit 主要负责：

- NSPanel/NSWindow 管理
- 全局快捷键
- 菜单栏生命周期
- Accessibility 和跨应用选区
- 更底层的 macOS 交互

### 6.2 模型运行时

原型阶段：

- 使用 Ollama，Swift 应用通过本地 HTTP API 调用。

正式分发阶段：

- 考虑内嵌 llama.cpp 或 MLX。
- 模型首次启动时单独下载，不直接塞进 `.app` 或 DMG。
- 模型文件、许可证、来源和 SHA-256 校验信息保存在 Application Support。

最终产品的默认模型不再以 4B 为目标。模型只需要擅长受限工具路由、参数提取、澄清和短结果解释，不承担通用知识问答。

当前模型目标：

- 基础模型约 0.3B～1.7B 参数。
- 优先验证 0.6B 级别模型；不够稳定时再升级到 1.5B～1.7B。
- 4-bit 量化后的下载文件目标不超过 1GB，理想目标为 350～700MB。
- 默认上下文限制在 1K～2K，不为工具路由保留超长上下文。
- 路由输出通常不超过 30 tokens，并且只允许结构化 JSON。
- 结果解释通常不超过 80 tokens。
- 关闭思考模式，禁止输出长推理过程。
- 不把 4B、7B 或 8B 模型作为默认常驻组件；未来如需通用聊天，可作为用户主动下载的可选模型或云端能力。

需要明确：LoRA/QLoRA 微调不会自动让一个 4B 模型变成小于 1GB。正确做法是选择原本就很小的基础模型，对它进行微调、合并和量化。

第一阶段仍然不立即训练模型。先证明工具体系可用，再决定是否对小模型做 LoRA/QLoRA 微调。改进顺序：

1. 工具描述
2. Prompt
3. JSON Schema 和结构化输出
4. 参数校验
5. 正确示例
6. 更换现成模型
7. 积累失败数据
8. 最后才考虑 LoRA/QLoRA 微调

微调数据主要训练以下能力：

- 从 3～5 个候选工具中选择正确工具。
- 从中文口语、模糊表达、否定句和省略句中提取参数。
- 在必要参数缺失时询问，而不是猜测。
- 正确区分只读、修改和高风险任务。
- 在修改任务前返回确认请求。
- 把结构化工具结果改写成简短、准确、不夸大的解释。

不应从零预训练基础语言模型。第一版可以直接使用现成的 0.6B 或 1.5B～1.7B 指令模型，收集真实错误后再微调。

### 6.3 模型和工具的职责边界

模型负责：

- 理解意图
- 选择工具
- 提取参数
- 生成、翻译、总结和解释文本

普通程序负责：

- 搜索和修改文件
- OCR
- 内部系统探针和组合诊断
- 剪贴板存储
- 权限检查
- 事务、日志和撤销

模型不能直接假装执行工具，也不能直接执行任意 Shell。

### 6.4 Tool Registry 与路由约束

架构必须分成两层：

```text
用户任务 Tool Registry
→ 组合调用内部 Probe Registry / Service
→ 返回面向任务的结构化结果
```

用户任务是模型和命令解析器可以选择的能力，例如 OCR、翻译、查找文件、整理文件、性能诊断。内部探针是实现细节，例如读取内存压力、Swap、磁盘、进程和端口；它们没有公开命令别名，也不进入模型候选工具列表。

每个面向用户的工具至少声明：

- 唯一标识和用户可见名称
- 命令别名
- 功能描述和适用边界
- 参数 JSON Schema
- 风险级别
- 所需系统权限
- 是否只读
- 是否必须预览和确认
- 超时、取消和失败行为
- 结构化返回 Schema
- 可用的确定性结果模板

每个内部探针至少声明：

- 唯一标识
- 输出 Schema 和数据单位
- 所需权限
- 超时和失败行为
- 数据采集时间
- 是否可能产生明显性能或隐私开销

`la ask` 不应把全部工具说明一次性塞给模型。先通过规则、关键词、Embedding 或工具分类召回最相关的 3～5 个，再由小模型做最终选择。这既减少延迟和上下文，也提高小模型准确率。

### 6.5 Skill Registry 与工作流执行引擎

用户技能必须和底层工具分开建模：

```text
Tool Registry
→ 开发者实现并审查的原子能力

Skill Registry
→ 用户创建的声明式工作流
→ 只引用 Tool Registry 中允许的工具
```

建议的 Skill Definition 至少包含：

- 稳定 ID、名称、图标、描述和搜索别名。
- 输入来源，例如选中文字、剪贴板、文件、截图或手动参数。
- 有类型的步骤列表或有向无环图。
- 每一步的工具 ID、参数绑定、输入输出类型和失败策略。
- 少量条件分支；循环、并行和定时触发后续逐步开放。
- 所需权限、网络域名、风险等级和确认策略。
- 输出方式，例如面板显示、复制、保存文件或通知。
- 创建时的原始自然语言、生成模型、版本、测试样例和修改历史。

创建链路：

```text
用户一句话描述
→ 检索可用工具、触发器和输出
→ 模型追问缺失参数
→ 生成受 JSON Schema 约束的 Skill Definition
→ 静态类型检查、权限和风险分析
→ 转换成人类可读的执行计划
→ 使用样例或真实只读数据测试
→ 用户确认保存
→ 写入 Skill Registry 和搜索索引
```

运行时不让模型重新发明流程。已经保存的技能由确定性执行引擎直接运行；只有步骤明确包含文本生成，或遇到需要语义判断的条件时才调用模型。

### 6.6 本地执行与云端 AI 技能

创建技能前必须先让用户选择运行策略，两种策略共用同一个 `Skill Definition`、Tool Registry、权限系统和执行引擎：

- `localOnly`：运行时禁止调用 `model.*` 和 `cloud_api`，但不等于禁止网络。技能可以使用经过注册和授权的本地网络工具；必须声明目标主机、用途和可能离开本机的数据。
- `cloudAssisted`：允许组合本地工具和统一的 `model.generateText`。技能保存的是以后运行时使用的提示词模板、输入变量和数据发送说明，默认使用用户设置的模型服务商，不绑定 DeepSeek、GLM、Gemini 或 OpenAI。

必须分别展示“创建技能时使用的模型”和“技能运行时是否调用云端”。一个技能可以由云端模型辅助设计，但保存后完全在本地执行；这不代表创建过程本身是离线的。

两种运行路径：

```text
localOnly
→ 触发器 → 本地工具/受限工作流 → 结果

cloudAssisted
→ 触发器 → 本地工具（可选）→ model.generateText → 本地输出（可选）
```

本地模式的网络访问必须通过具体工具和域名白名单表达，不能退化成任意 Shell 或不受控 Socket 权限。云端模式必须生成 `modelTask.promptTemplate`，并逐项声明哪些输入会发送给云端。

## 7. 性能、功耗和内存约束

产品必须做到待机无感：

- 没有任务时不持续推理。
- 没有任务时不创建复杂主界面。
- 文件监听使用系统事件，不轮询磁盘。
- 剪贴板只在变化后处理一次。
- 大规模索引尽量仅在插电和系统空闲时运行。
- 简单任务不加载 LLM。
- 0.3B～1.7B 路由模型闲置 5～10 分钟后自动卸载。
- 可选的大模型只在用户明确启用通用聊天或复杂任务时加载。
- 低电量模式禁止加载大模型。
- 内存压力升高时自动卸载模型。
- 默认限制上下文和输出长度。
- 长文档使用切块、检索和 RAG，而不是无限扩大上下文。
- 未来的主动智能优先监听系统事件；无法监听事件时才使用低频、可调节、可暂停的探针采样。
- 正常状态下只运行规则或轻量分类，不持续调用生成模型；检测到候选异常后才按需唤醒模型生成解释。
- 主动提醒必须去重、限频，并遵守勿扰模式、安静时段、电池状态和用户设置。

建议功耗模式：

- 节能：0.3B～0.6B、模板解释、短上下文、快速卸载、后台索引仅插电运行。
- 平衡：0.6B～1.7B、短文本解释、闲置 5～10 分钟卸载。
- 高质量：仍优先保持工具执行确定性；通用聊天模型作为单独可选下载，不改变核心工具链。

针对当前 16GB M5 MacBook Air 的规划目标：

| 调用类型 | 目标延迟 | 典型资源特征 |
|---|---:|---|
| `la translate`、文件查找等直接命令 | 0.1～0.5 秒起 | 不为路由加载语言模型；实际耗时取决于任务 |
| Apple Vision OCR | 普通图片约 0.2～1 秒 | 使用系统视觉能力 |
| `la ask` 小模型工具路由 | 热启动约 0.1～0.4 秒 | 只输出短 JSON |
| 路由 + 工具 + 模板结果 | 热启动约 0.4～1.2 秒 | 常用路径目标 |
| 路由 + 工具 + 模型解释 | 热启动约 0.5～2 秒 | 两次短推理 |
| 模型冷启动完整请求 | 约 1.5～4 秒 | 包含加载和首次计算 |

以上是工程目标，不是实测承诺，必须在真实模型、真实 MLX/llama.cpp 运行时和发布构建上建立基准测试。

模型文件和运行内存不是一回事。约 0.6B～1.7B 的 4-bit 模型文件可能为 350MB～1.2GB，连同 KV Cache、临时张量和运行时，整体常驻内存目标约 0.7～2.5GB。保持模型已加载主要占用内存，不代表持续占用 GPU；没有请求时不得持续生成或轮询。

一次短请求预计只形成几秒以内的 CPU/GPU 计算脉冲，不应造成明显发热。实际功耗必须使用 Instruments、`powermetrics` 或等效方法在电池和插电模式分别测量，不能只根据模型参数量推断。

Apple Silicon 注意事项：

- Ollama、llama.cpp 和 MLX 的 LLM 主要使用 Metal GPU、CPU 和统一内存，不等于主要使用 Neural Engine。
- Neural Engine/Core ML 更适合 OCR、Embedding、小型分类、语言检测、敏感信息检测、语音唤醒等任务。
- 不应为了“使用 NPU”而强制所有模型跑在 Neural Engine。
- 内存带宽、量化、上下文长度和统一内存压力通常比宣传 TOPS 更重要。

## 8. 安全约束

所有会修改用户数据的操作都必须经过统一流程：

```text
理解请求
→ 生成计划
→ 验证路径和参数
→ 冲突检测
→ 显示预览
→ 用户确认
→ 执行
→ 写入日志
→ 支持撤销
```

强制要求：

- 工具白名单。
- 严格 JSON Schema 和参数校验。
- 目录作用域限制。
- 禁止普通用户模式下的任意 Shell 执行。
- 禁止修改系统目录。
- 文件冲突时停止或明确询问。
- 删除默认进入废纸篓，不永久删除。
- 批量操作必须预览。
- 文件移动、重命名必须支持撤销。
- 结束进程、修改启动项等高风险操作必须确认。
- 模型不确定时停止，不猜测执行。
- 第三方生成或导入的技能默认不得执行任意 Shell，也不得绕过统一 Tool Registry 和权限系统。
- 导入技能前必须展示其触发条件、读取数据、网络访问、可修改范围和所需权限。
- 每个主动规则和技能都必须可以单独暂停、禁用和删除，并保留可审计的触发与执行记录。

## 9. 隐私约束

- 默认完全本地运行。
- 默认不需要账号。
- 默认不上传文件、剪贴板、截图和对话。
- 不做遥测，或者未来遥测必须明确选择加入。
- 明确展示应用可以访问的目录和权限。
- 剪贴板内容可设置自动过期。
- 支持临时会话，关闭后不保留历史。
- 保存完整但可管理的操作日志。
- 敏感内容过滤必须在本地完成。
- 语义索引、Spotlight 索引和模型缓存均需要可清除。
- “模型在本地”不等于自动拥有完整隐私；监控范围、数据留存、通知内容和第三方技能同样必须受控。
- 主动智能默认关闭，按场景和应用逐项选择加入，不默认监控所有窗口、消息或通知。
- 消息提醒优先使用应用提供的公开集成或用户明确授权的数据源，不默认持续抓取整个屏幕。
- 第三方大模型生成技能时，默认只发送用户对功能的描述和必要 Schema，不发送本地运行数据、消息内容或文件内容。
- 如果某个自定义技能需要云端模型，执行前必须明确显示将发送的数据、目标提供商和保留风险。

## 10. 发布与工程注意事项

- 当前优先支持 macOS 和 Apple Silicon，不同时展开 Windows/Linux。
- 因为 Accessibility、模拟输入和广泛文件访问，正式版本可能更适合 Developer ID + 公证后在网站分发，而不是一开始挑战 Mac App Store Sandbox。
- 正式分发需要 Apple Developer Program、Developer ID、Hardened Runtime 和 Apple 公证。
- 模型许可证和量化文件来源必须逐项核对。
- Apache 2.0 原模型不代表任何第三方量化产物都能不经核查直接重新分发。
- 最终应用应支持模型和程序分别更新，避免每次更新重新下载数 GB 模型。

## 11. 当前工程状态

当前工程是 UI 原型，使用 Xcode 27 构建成功。

已经实现：

- 原生 macOS SwiftUI 工程。
- 菜单栏图标。
- `Option + Space` 全局快捷键。
- 无标题栏和窗口按钮的 Spotlight/Raycast 风格轻量主面板，尺寸约 780×510。
- 主面板失去焦点时自动隐藏，支持 `Esc` 关闭，并由快捷键重新唤起。
- macOS 26 及以上使用原生 Liquid Glass 交互材质，旧系统使用系统 Material 回退。
- 突出的统一搜索/问答框。
- 四项根据最近点击持久化排序的推荐技能。
- 输入时根据工具命令名、中英文别名和描述实时预测补全，最佳匹配置顶。
- 面板显示期间临时使用英文输入源，隐藏或退出时恢复此前输入法。
- 模拟的意图匹配、加载状态、问答结果、复制结果和重新提问流程。
- 当前推荐技能包含 OCR、选区总结、文件查找和改写，并可随最近使用变化。
- 设置窗口。
- 设置入口已从不稳定的 `SettingsLink` 改为显式持有的原生 `NSWindow`。

当前没有实现：

- App Intents
- Spotlight 动作
- Ollama 或其他模型
- OCR
- 选中文字
- 剪贴板历史
- 文件操作
- 系统状态工具
- 权限申请
- 数据库
- 主面板中的技能和问答目前仍为演示数据，尚未连接 Tool Registry、系统能力或本地模型。

已知非阻塞日志：

```text
CSInlineDonation / SetStoreUpdateService
```

该日志来自 macOS/Xcode Beta 的系统搜索/索引服务，不是设置窗口无法打开的根因。设置窗口问题已经通过显式窗口控制器处理。

## 12. 推荐的后续开发顺序

1. 确定最终产品名称并完成工程重命名。
2. 建立统一 Tool Registry、参数类型、权限、预览、确认、日志和撤销框架。
3. 添加第一个 App Intent：打开应用/打开自由指令窗口，验证 Spotlight 唤醒链路。
4. 接入 Apple Vision OCR，完成第一条真实工具调用链。
5. 完成选区文字读取、翻译、文件查找等 3～5 个安全原子工具。
6. 建立 Skill Definition、Skill Registry 和确定性工作流执行引擎，先支持顺序步骤和输入输出绑定。
7. 用手工 JSON/内置模板创建并运行两个组合技能，先证明执行引擎可靠，不急着接生成模型。
8. 接入可插拔模型 Provider，支持内置本地模型和用户自己的 API；密钥只存 Keychain。
9. 实现自然语言技能创建 MVP：追问、结构化生成、静态检查、计划预览、试运行和保存。
10. 将保存的用户技能接入 `Option + Space` 搜索、最近使用、编辑和禁用界面。
11. 将 `UserSkillEntity` 写入 Core Spotlight，验证系统搜索发现和通用执行入口。
12. 实现 `la ask` 的候选工具/技能召回、模型选择和参数校验。
13. 建立内部 Probe Registry 和 `diagnose_performance`，但不把底层探针暴露为用户命令。
14. 再扩展剪贴板、文件修改、条件分支、触发器和主动智能。
15. 建立创建成功率、执行成功率、路由准确率、冷/热延迟、峰值内存和单次能耗基准，收集失败样本后再决定是否微调。

## 13. 不应偏离的产品原则

1. 结果优先于聊天过程。
2. 简单任务不调用大模型。
3. 没有任务时尽量不占用 CPU/GPU。
4. 修改用户数据前必须预览和确认。
5. 所有修改尽量可撤销。
6. 默认本地和私密。
7. Spotlight 是主要入口之一，而不是唯一入口。
8. 不依赖一个长期打开的大型主界面。
9. 不为展示 AI 而滥用 AI。
10. 新增功能必须先考虑权限、失败方式、功耗和撤销。
11. 内置功能是安全原语和示例，不是限制用户能力的固定菜单。
12. 模型可以提出和生成计划，但只有经过 Schema、权限和风险校验的声明式技能才能执行。

## 14. 竞品与开源项目地图

> 调研日期：2026-08-19。竞品功能变化很快，正式立项、定价或发布前必须重新核查。这里记录的是产品定位和技术参考，不代表可以直接复制其代码；使用任何开源实现前必须检查许可证。

### 14.1 最接近的直接竞品

| 产品 | 开源情况 | 已覆盖能力 | 与本项目的关键差异 |
|---|---|---|---|
| [Raycast](https://manual.raycast.com/ai/ai-commands) | 核心闭源，扩展仓库开放 | 启动器、文件、剪贴板、用户自建 AI Commands、AI Extensions、自然语言选工具 | 已经能低门槛保存自定义 Prompt；更复杂扩展仍需要 TypeScript/Node 开发。本项目不能把“自定义命令”本身当创新，应突出一句话生成多步骤安全工作流、本地执行和可撤销操作 |
| [EnConvo](https://docs.enconvo.ai/workflows/creating-workflows) | 核心闭源，扩展开源 | 原生 macOS、SmartBar、OCR、本地模型、插件和可视化工作流画布 | 已支持很完整的动作、条件和循环，但用户仍需理解节点、变量并手工连线。本项目的机会是让非技术用户通过对话完成需求澄清、生成、测试和维护 |
| [Asyar](https://github.com/Xoshbin/asyar) | AGPL-3.0 | 本地优先启动器、剪贴板、文件搜索、工具调用、MCP、静默 AI 命令、扩展权限 | 很接近“本地 Raycast”；它仍是替代型启动器并允许通用 Agent/Shell，本项目计划复用 Spotlight，并以受限原生工具和可撤销操作为中心 |
| [Ainto](https://github.com/ainto-labs/ainto-app) | GPL-3.0 | 原生启动器、剪贴板、选区 AI、文本替换、Snippets | 体积和原生体验值得参考，但当前 AI 依赖 Claude Code，不是内置极小本地路由模型，系统诊断和安全工具链不是主线 |
| [Kivio](https://github.com/ZMGID/kivio) | GPL-3.0 | 全局快捷键、选中文字、翻译、截图 OCR、视觉问答、工具调用、Skills、MCP | 屏幕和聊天能力非常接近，但更像完整 Agent 客户端；本项目应避免发展成重型聊天应用 |

### 14.2 功能局部相似或技术方向相近

| 产品 | 相似点 | 不同点/可学习内容 |
|---|---|---|
| [OpenFelix](https://github.com/fspecii/openfelix) | MLX 本地模型、工具调用、快捷键、语音、视觉、定时任务 | 偏语音和通用 Agent，可参考本地模型路由与按需升级思路 |
| [macOS26/Agent](https://github.com/macOS26/Agent) | 原生 macOS、Apple 本地模型、文件与系统工具、工具日志 | 面向强 Agent 和自动化，权限面更大；可参考工具审计，但不能照搬任意 Shell 路线 |
| [Witsy](https://github.com/Kochava-Studios/witsy) | 全局快捷键、选区 AI、本地 Ollama、RAG、MCP 与工具调用 | 功能全面但偏跨平台通用 AI 客户端；证明“本地模型 + 系统级快捷动作”并不是空白市场 |
| [screenpipe](https://github.com/screenpipe/screenpipe) | 本地优先、Accessibility、OCR、SQLite、自然语言搜索 | 核心是 24/7 屏幕与音频记忆，官方给出的典型资源开销也更高；本项目不应默认持续录屏 |
| [TypeTide](https://github.com/everettjf/TypeTide) | macOS 选区读取、剪贴板回退、翻译、原位替换、本地 Ollama | 是文字处理这一条垂直功能的直接参考，尤其是保留剪贴板和原生撤销的做法 |
| [Alfred](https://www.alfredapp.com/help/overview/) | 文件搜索、剪贴板、Workflow、快捷启动 | 证明用户愿意为可靠的键盘效率工具付费，但需要用户学习工作流和命令，不是本地自然语言工具路由 |
| [Apple Spotlight / App Intents / Shortcuts](https://support.apple.com/guide/mac-help/use-apple-intelligence-in-shortcuts-mchl91750563/26/mac/26) | 系统原生入口、动作发现、快捷指令和 `Use Model` | Apple 已允许用户在快捷指令里调用本地、Private Cloud Compute 或扩展模型，但仍主要依赖用户寻找动作和搭建流程；本项目应降低创建门槛，并提供统一的技能搜索、权限解释和安全执行层 |

### 14.3 当前竞争结论

- “全局快捷键 + AI 对话 + 翻译/OCR/剪贴板”已经不新颖，单靠功能列表无法形成竞争力。
- “自然语言自动选择扩展工具”也已经被 Raycast、EnConvo、Asyar 和通用 Agent 实现，不能把 Tool Calling 本身当作创新。
- “用户自定义 AI 命令或工作流”同样已经存在。可争取的差异是把创建过程从填写 Prompt、编写扩展或拖拽节点，降为一句话描述、少量澄清、自动生成、自动测试和一键保存，同时保持本地优先与安全边界。
- 本项目仍有可以成立的组合差异，但属于产品和工程创新，而不是基础算法创新：
  - 内置功能作为可组合原语，用户创建的个人技能才是持续增长的产品能力。
  - 新技能保存后立即进入 `Option + Space` 搜索，并作为动态实体尽可能进入系统 Spotlight。
  - 不强迫用户更换 Spotlight 或学习一个大型新启动器。
  - `la 直接命令` 与 `la ask` 明确分流，已知任务完全不调用 LLM。
  - 使用目标小于 1GB 的专用本地路由模型，而不是为每个小动作调用大型通用模型或云端 API。
  - 把“电脑为什么卡”等问题导向的组合诊断做成一等功能，同时隐藏普通用户不需要单独调用的底层指标探针。
  - 工具输出结构化证据，解释必须忠于证据。
  - 文件和系统修改统一经过预览、确认、日志和撤销，而不是给 Agent 任意 Shell 权限。
  - 平时没有主窗口、没有持续推理、没有默认录屏，真正做到按需出现。

## 15. 产品是否足够让人使用

### 15.1 判断

这个方向存在真实需求，但目前的“大而全功能清单”本身还不足以让用户从 Raycast、Alfred、EnConvo 或系统自带工具迁移。用户不会因为“又一个什么都能做的 AI 助手”而长期留下，只会因为某几个每天发生的任务被明显做得更快、更可靠、更私密而留下。

需求信号是存在的：截至调研日，Raycast Pro 以订阅方式销售 AI 能力，Alfred Powerpack 仍以付费许可证销售，EnConvo 同时销售许可证和云服务；开源侧的 screenpipe 约有 20.6k GitHub stars，Witsy 约有 2k stars。与此同时，更接近“本地 Raycast”的 Asyar 约 238 stars、Ainto 约 18 stars，说明“做出来”不等于“会被大量使用”，入口、稳定性、品牌和高频刚需比功能数量更重要。

项目值得继续做，但首个可发布版本必须定位为：

```text
一个藏在 macOS 里的本地操作层：
内置工具解决常见任务，用户用一句话创建自己的搜索技能；
明确命令立即执行，模糊请求由模型理解，所有结果有证据，所有修改可撤销。
```

不应在第一版宣传“什么都能做”。更可信的表达是：

```text
Tell your Mac what you need.
Fast commands. Private intelligence. Safe actions.
```

### 15.2 第一版必须形成的四个高频记忆点

1. **个人能力生成**：用户用一句话描述重复任务，产品通过澄清、计划预览和试运行把它保存成自己的搜索技能，不要求理解节点、变量或代码。
2. **系统医生**：`la ask 为什么电脑这么卡` 能在约 1～2 秒内指出真实的内存、Swap、CPU 或磁盘原因，并给出下一步建议。
3. **瞬时处理**：`la ocr`、翻译、总结和文件查找等直接动作不为路由加载语言模型，尽量在 0.5～1 秒内给出反馈。
4. **安全行动**：整理、重命名和移动文件时先展示清晰 diff，用户确认后执行，并且能够撤销。

OCR、翻译、剪贴板和本机诊断是让用户快速理解产品价值的示例能力；“一句话生成个人技能 + 可信的本地执行 + 可撤销操作”才是更适合作为长期差异化核心的组合。

### 15.3 第一版不应做的事情

- 不先做一个功能繁多的聊天主窗口。
- 不先做 100 个质量不稳定的工具。
- 不允许模型生成并直接执行任意 Shell。
- 不默认持续录屏、录音或索引整个硬盘。
- 不用 4B 以上模型证明产品有 AI。
- 不把 Spotlight 的非确定性匹配隐藏起来，应始终保留可靠的快捷键入口。
- 不为了“全能”同时开发 macOS、Windows 和 Linux。

### 15.4 建议的 V1 功能边界

首个可用版本建议只做好 6～10 个工具：

- `ocr`
- `translate`
- `summarize`
- `explain`
- `rewrite`
- `clipboard_search`
- `diagnose_performance`
- `find_file`
- 一项带预览和撤销的文件重命名/移动能力
- 一个受限的自然语言技能创建器：只组合已经注册的工具，支持顺序执行、输入输出绑定、计划预览、试运行、保存、禁用和删除。

内存、磁盘、进程、端口和网络读取作为 `diagnose_performance` 等组合任务的内部探针实现，不计入公开工具数量，也不直接出现在 Spotlight、快捷面板或模型候选列表中。这些用户任务足够验证直接命令、`la ask`、权限、安全、结构化结果和模型路由，不需要先完成完整 Agent 平台。

### 15.5 发布前的可量化门槛

- 直接命令 P95 响应小于 500ms，OCR 除外。
- 热启动 `la ask` 常见系统诊断 P95 小于 2 秒。
- 常见单工具请求选择准确率目标高于 95%。
- JSON Schema 合法率目标高于 99%。
- 所有修改型工具的确认触发率必须接近 100%。
- 待机时 CPU 接近 0，不发生持续模型推理。
- 小模型下载文件不超过 1GB。
- 新用户在 60 秒内能够完成第一个直接命令。
- 对 20～30 名真实用户测试后，至少有一个功能被多数目标用户每周重复使用；否则继续收窄定位，而不是增加更多功能。

### 15.6 可参考但不能直接照搬的工程项目

- 选区和剪贴板回退：[TypeTide](https://github.com/everettjf/TypeTide)
- 原生启动面板与 Swift/Rust 分层：[Ainto](https://github.com/ainto-labs/ainto-app)
- 工具权限和敏感剪贴板过滤：[Asyar](https://github.com/Xoshbin/asyar)
- Accessibility 优先、OCR 回退和本地索引：[screenpipe](https://github.com/screenpipe/screenpipe)
- Apple Silicon 本地模型与工具调用：[OpenFelix](https://github.com/fspecii/openfelix)

这些项目的 GPL、AGPL、Apache 或其他许可证各不相同。除非明确接受其许可证影响，否则只学习产品行为和公开架构，不复制代码。

## 16. 远期方向：主动智能与复杂技能生态

### 16.1 产品含义

主动智能不是让模型一直观察电脑或持续推理，而是让应用在用户授权的范围内注意重要变化，只在真正需要时打断用户。

目标交互：

```text
系统事件 / 应用事件 / 低频探针
→ Trigger Engine 规则和轻量检测
→ 去重、冷却时间和隐私策略
→ 必要时调用用户任务或小模型解释
→ 本地通知或临时结果窗口
```

主动智能应复用现有内部 Probe Registry、Tool Registry、安全策略和操作日志，不能建立第二套绕过权限的自动化系统。

### 16.2 候选场景

- 系统在一段时间内出现异常内存压力、Swap、CPU、磁盘或网络状态，通知用户原因和建议。
- 某个进程持续异常占用资源，而不是因为一次瞬时峰值就通知。
- 支持的消息应用出现用户明确关心但数分钟仍未查看或回复的消息，按照用户规则提醒。
- 下载、导出、转码、备份或其他长任务完成或失败时主动通知。
- 用户指定的目录出现符合规则的新文件时，提出整理建议，但默认不自动移动。
- 根据用户保存的工作流进行本地定时检查和提醒。

消息场景必须区分“未读”“已读但未回复”“无需回复”，并允许按应用、联系人、关键词、时段和提醒延迟配置。第一阶段不承诺跨所有消息应用通用实现，应从具有可靠 API、通知事件或明确授权集成的应用开始。

### 16.3 通知质量要求

- 默认宁可少提醒，避免把产品变成新的通知噪音源。
- 同一问题在冷却时间内合并，不重复轰炸。
- 通知必须说明触发原因，并允许查看证据。
- 用户可以直接选择“以后不提醒此类情况”“延后提醒”或“调整规则”。
- 不在锁屏通知中默认显示敏感消息或文件内容。
- 每条通知都能追溯到具体规则、数据来源和判断时间。

### 16.4 第三方大模型生成并导入技能

远期允许用户用自然语言向自己选择的第三方大模型描述需求，例如：

```text
工作日下午，如果重要联系人发来消息后十分钟还没回复，就提醒我；
但会议期间不要提醒。
```

第三方模型应生成声明式 Skill Package，而不是一段获得任意权限的代码。建议包结构包含：

- 名称、版本和作者
- 自然语言说明
- 触发条件
- 允许调用的现有 Tool/Probe
- 参数 Schema
- 所需权限和网络域名
- 是否读取敏感数据
- 是否修改本地数据
- 确认策略
- 频率、冷却时间和安静时段
- 测试样例和预期结果
- 可选签名、来源和完整性校验

导入流程：

```text
第三方模型生成技能包
→ 本地 Schema 校验
→ 静态权限和风险分析
→ 展示人类可读预览
→ 用户逐项授权
→ 沙箱或受限运行时测试
→ 启用
→ 全程记录并可立即停用
```

自定义技能优先组合应用已有的安全工具。只有未来建立成熟沙箱后，才考虑受限脚本；普通模式始终禁止无约束 Shell 和未声明网络访问。

### 16.5 实现优先级

本节的优先级只针对主动智能、持续触发和复杂第三方集成。基础的“一句话创建受限个人技能”已经提升为核心产品能力，应按照第 12 节的顺序提前开发，不必等待主动智能完成。

主动智能应在直接命令、`la ask`、Tool Registry、权限系统、日志和撤销已经稳定后再开发。推荐顺序：

1. 本地长任务完成通知。
2. 只读系统异常规则提醒。
3. 用户手工配置的定时检查。
4. 单个具有可靠接口的消息应用提醒。
5. 声明式工作流导入。
6. 第三方大模型生成 Skill Package。
7. 经过充分安全验证后再扩展跨应用主动能力。

## 17. 核心产品决策：一句话创建自己的搜索技能

### 17.1 可行性结论

这个方向技术上可行，也比继续堆叠固定功能更有产品价值。它能让 `Option + Space` 从“打开某个软件的快捷键”变成“打开用户自己的个人命令层”，更有机会形成长期使用习惯。

但目标应定义为：

```text
自然语言 → 受约束的声明式工作流 → 校验与试运行 → 保存为个人搜索技能
```

而不是：

```text
自然语言 → 模型自由生成代码 → 直接控制整台电脑
```

后者难以保证权限、兼容性、可解释性和撤销，也会让一个模型错误直接变成数据损失风险。

### 17.2 产品交互

建议提供固定入口 `create` 或“创建新技能”：

```text
用户：以后我输入“会议收尾”，就把当前选中的会议记录总结成待办并复制。

系统：我会创建以下流程：
1. 读取当前选中文字
2. 提取待办事项
3. 显示结果
4. 用户确认后复制到剪贴板

需要权限：辅助功能、剪贴板
修改风险：低
是否使用云端：否

[试运行] [保存技能] [修改]
```

保存后，“会议收尾”及模型生成的别名立即出现在快捷面板预测中。技能详情页必须允许用户查看自然语言说明和底层步骤、修改参数、查看最近运行记录、导出、禁用和删除。

### 17.3 模型分工

- 小型常驻模型继续负责已存在工具和技能的路由，不负责复杂工作流设计。
- 简单技能创建可以使用内置模型配合模板和结构化生成。
- 复杂技能创建属于低频、用户主动触发任务，可以临时加载更强的可选本地模型，或使用用户自己的 API。
- 云端 Provider 只接收用户的功能描述、允许使用的工具 Schema 和必要的澄清信息；默认不上传真实剪贴板、文件、截图和运行历史。
- Apple Foundation Models 在可用设备和地区可以作为可选 Provider，其 guided generation 能生成受约束结构；产品不能依赖它作为唯一实现：[Foundation Models guided generation](https://developer.apple.com/documentation/foundationmodels/generating-swift-data-structures-with-guided-generation)。

不能假设 0.6B 路由模型可以稳定设计任意多步骤自动化。MVP 应限制工具数量和流程复杂度，用 Schema、模板、候选动作召回和样例测试提高成功率；复杂请求如果缺少原子工具，应明确回答“当前还不能创建，因为缺少某项能力”，而不是编造实现。

### 17.4 分阶段范围

第一阶段：Prompt Skill

- 选中文字/剪贴板 → 固定提示 → 模型处理 → 显示、复制或替换。
- 最接近 Raycast AI Commands，但创建过程由一句话和澄清完成。

第二阶段：组合 Skill

- 组合 OCR、翻译、总结、文件查找、剪贴板和安全保存。
- 支持顺序步骤、参数引用和有限条件。

第三阶段：系统与应用集成

- 通过 App Intents、公开 API、URL Scheme 或经过审查的 Connector 操作其他应用。
- 每个 Connector 明确声明读取、写入、网络和身份权限。

第四阶段：触发器和主动技能

- 文件事件、定时条件、长任务完成和用户授权的消息事件。
- 增加限频、安静时段、去重和审计。

第五阶段：可分享 Skill Package

- 导入、导出、签名、权限差异提示、版本更新和社区市场。
- 第三方技能不能绕过统一执行引擎。

### 17.5 主要风险

- 用户描述模糊：必须追问，不自动猜测路径、联系人和修改范围。
- 模型生成错误：必须通过 Schema、类型检查、权限检查和试运行。
- 原子工具不足：技能生成器的真实能力永远受 Tool Registry 限制，需要明确展示缺失能力。
- 复杂流程失控：V1 禁止无限循环、任意代码、任意 Shell 和未声明网络请求。
- 云端隐私：创建阶段和运行阶段的数据必须分离，Provider 配置显示将发送什么。
- 维护成本：工具 Schema 变化时需要技能迁移、版本兼容和失效提示。
- Spotlight 预期过高：系统入口只做动态实体发现，完整创建、补全和执行体验由自有面板保证。

### 17.6 验证指标

- 新用户能否在 60 秒内用一句话创建并成功运行第一个技能。
- 在无需人工编辑 JSON 或节点图的情况下，简单技能首次生成成功率是否达到 80% 以上。
- 保存后的技能是否在一周内被重复使用，而不只是创建后闲置。
- 每个修改型技能是否 100% 正确触发预览和确认。
- 失败时是否能指出缺少的权限、参数或原子工具，而不是生成不可执行流程。
- 用户是否愿意把 `Option + Space` 长期留给产品；这是比下载量更关键的入口价值指标。

## 18. 2026-08-20 核心功能原型实现状态

Demo 版本提交后，工程开始进入可运行的核心功能原型阶段。当前已经实现：

- 统一 `AIProvider` 配置，首批支持 DeepSeek、智谱 GLM、Google Gemini 和 OpenAI；Claude 暂不接入。
- DeepSeek/GLM 使用 Chat Completions 风格适配，Gemini 和 OpenAI 各自使用独立适配器；当前透明流式技能生成已接通 DeepSeek 与 Gemini，GLM 和 OpenAI 仍只有普通请求适配，后续再补流式生成。
- API 地址和模型名称可修改，避免把会变化的模型版本永久写死。
- API Key 只保存在 macOS Keychain；普通配置保存在 `UserDefaults`。
- 设置页支持选择默认服务商、编辑端点/模型、保存并发起真实连接测试。
- 未匹配固定命令的普通问题，在已配置 API 时会显示真实模型回答。
- 菜单栏和快捷面板增加“创建技能”入口。
- 技能创建器支持“引导模式”和“自由模式”。引导模式改为四步流程：先设置功能名称与注册关键词，再定义运行时参数的数量、类型和必填状态，然后描述处理过程，最后定义输出形式与内容；每一步完成后再进入下一步。
- 自由模式面向熟悉提示词的用户：上方直接描述完整需求，下方仍必须明确注册关键词，并可增加文本、文件、图片、文件夹、数字或布尔参数，避免自然语言提示词与实际调用协议脱节。
- 技能参数已经进入持久化 Schema、模型生成提示词和技能预览；旧技能缺少参数字段时按无参数兼容。
- `workflow.arguments` 使用可递归的 JSON 值类型，兼容字符串、整数、小数、布尔值、数组、嵌套对象和 `null`；不能再用 `[String: String]` 假设所有工具参数都是纯文本。生成提示词必须同时明确各工具的参数结构，`modelTask` 的 `promptTemplate`、`inputVariables`、`providerPolicy` 不得重复塞入工作流参数。
- 快捷面板匹配到带参数的技能后会进入参数收集状态。文本参数可跟在命令后传入；文件、图片和文件夹支持 Finder 拖拽、剪贴板粘贴与原生 `NSOpenPanel` 选择。收集文件时面板临时保持置顶，结束或取消后恢复失焦隐藏。
- 文件参数目前只完成可靠的接收、类型检查和运行上下文绑定；统一模型接口仍是纯文本通道，不会把文件路径冒充文件内容。后续需由 Apple Vision / 文件读取工具先产出内容，或为 Gemini 等服务补多模态请求适配。
- 设置窗口采用类似 Xcode 的左侧分类导航与右侧详情布局，必须使用系统全尺寸内容视图和统一透明标题栏，让红黄绿窗口按钮自然位于侧边栏材质区域。左栏使用不可折叠的固定宽度自定义布局，不能使用会自动生成 Sidebar Toggle 的 `NavigationSplitView`，也不能通过延迟删除按钮造成闪现。左栏不显示多余的“设置”分组标题，使用 macOS 27 自适应的 Regular Liquid Glass，而不是固定深色材质。右侧顶部使用单一紧凑导航栏：Liquid Glass 前进/后退控件右侧显示当前分类名称；进入技能编辑时，同一导航栏改为返回技能列表、技能名称和启用开关，不能再额外增加第二条标题栏和重复返回按钮。技能管理不能在设置导航右侧继续嵌套两列：右侧先显示技能列表，点击技能后在同一区域进入二级编辑页，避免为了三列内容强行放大窗口。用户可以搜索、启用或停用、编辑名称/别名/说明/执行模式/步骤/参数/运行时提示词，保存修改，并在二次确认后删除技能。
- 所有面向用户展示的模型自然语言统一按安全 Markdown 渲染，不能用普通 `Text` 暴露 `**`、`#`、列表符号等原始标记。主面板问答、技能生成思考过程、技能预览解释和生成历史共用同一个渲染器。实时技能定义、未解析 JSON 和运行时提示词使用独立代码块组件，提供语言标记、横向滚动、文本选择和复制；不执行 HTML，也不加载 Markdown 远程图片。
- 主面板的 AI 回答直接排版在内容层，不添加一块颜色不同的回答卡片背景。推荐和预测列表每项下方不显示抽象功能说明，而显示真实调用格式，例如 `exp 单词`；用户技能需要持久化注册关键词，并根据参数名称生成调用示例，可选参数使用方括号表示。
- 技能可导出为版本化的 `.laskill` 文件。导出包包含格式标识 `com.localassistant.skill`、Schema 版本、应用版本、导出时间和完整技能定义，不包含 API Key、生成历史或运行时输入文件。未来社区、导入、签名、作者、许可证和依赖检查必须基于这个版本化容器演进，不能直接共享内部 `skills.json`。
- 技能生成结果包含名称、别名、说明、触发方式、步骤、输出、依赖能力、权限和自然语言解释。
- 没有 API 时仍可用本地模板生成草稿，便于完整体验界面流程；这不等同于模型理解。
- 用户确认后的技能以 JSON 保存在 `Application Support/LocalAssistant/Skills/skills.json`，不写入 App 包。
- 保存后的技能立即进入快捷面板搜索和预测；纯文本模型类技能可以调用当前 API 试运行。
- 依赖 OCR、文件、剪贴板、选区等尚未实现工具的技能，只展示真实执行计划和缺失能力，不允许模型伪造执行结果。
- 已建立首个最小可运行 `ToolRegistry` 与顺序 `WorkflowEngine`，当前正式注册 `clipboard.readText`、`model.generateText`、`clipboard.writeText`。工作流支持 `{{参数名}}`、`$步骤变量`、`saveAs`、取消、逐步错误传播和脱敏耗时日志。
- “剪贴板翻译”已经完成第一个本地—云端—本地闭环：读取剪贴板纯文本，调用用户当前选择的云端模型，将最终译文写回剪贴板并同时在主面板显示。写入后会立即回读验证；剪贴板为空或写入失败时显示明确错误。
- 早期版本生成的 `clipboard + translate` 技能即使没有 `workflow` 和 `modelTask`，运行时也会由兼容层转换成三步标准工作流，因此用户不需要重新创建原有的 `trans` 技能。
- Tool Executor 日志只记录步骤、参数字段名、文字长度和耗时，不记录剪贴板原文或模型结果正文。Debug 构建提供 `--run-skill-debug <关键词>` 入口，可绕过 UI 对真实工作流做端到端验证；Release 构建不包含该入口。
- 技能管理的列表和二级编辑页必须形成真正的本地导航历史：从编辑页后退到列表后，前进按钮应立即可用并能返回原技能；打开其他技能会清空该前进分支。
- 普通自然语言技能运行时，系统提示词必须要求直接输出面向用户的 Markdown，并明确禁止用 JSON/XML 或代码围栏包装普通文字。若服务商仍误回纯 JSON 或 `json` 围栏，执行层自动转换为可读 Markdown；只有技能提示词或输出规范明确要求 JSON 时才保留结构化结果。
- 快捷面板不能再使用“整块窗口背景都可拖动”的方式，否则会吞掉 Markdown 正文的文字框选。窗口仅允许从顶部专用留白区域拖动，搜索、建议、参数和回答区域保持正常点击与选择行为。
- 快捷面板必须始终坚持“顶部唯一输入线”：不能在下方生成第二个文本框、参数表单或任何其他输入控件。命令候选被高亮时，用户按空格应立即锁定当前技能、补齐规范关键词和分隔空格，并在顶部输入框内用灰色占位提示下一参数；`Tab` 继续负责循环并预选候选，预选后键入同样进入该单行参数状态。用户开始输入当前参数后隐藏其占位提示，多参数依次在同一行继续填写。下方区域只允许显示候选、调用格式、下一参数及缺失项等只读指引。文件选择、粘贴和拖入入口也必须位于顶部输入区域。若回车时必填参数已齐全，应立即执行；缺失时保持顶部焦点并提示缺失项，不得跳转到下方参数页要求二次输入。

本阶段仍未完成：

- Apple Vision OCR、跨应用选中文字、文件搜索/整理、剪贴板历史和系统诊断执行器。
- Tool Registry 的完整描述 Schema、工具级参数类型检查、风险分级、权限代理、撤销和持久化审计；最小注册表、顺序执行器和剪贴板工具已经完成。
- Spotlight App Intents / Core Spotlight 动态技能实体。
- 本地小模型运行时。
- 技能包导入、技能版本历史和运行历史页面；搜索、编辑、启停、删除与 `.laskill` 导出已经完成。

下一阶段应在现有执行内核上接入 Apple Vision OCR：先处理用户拖入或选择的图片，再增加区域截图。OCR 输出必须作为结构化步骤结果传给 `model.generateText` 或 `clipboard.writeText`，不能在主面板重新写一套专用执行逻辑。
